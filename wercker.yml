box: node
build:
  steps:
    - npm-install
push:
  steps:
    - internal/docker-push:
        username: $USERNAME
        password: $PASSWORD
        repository: tadakamalla/testnode
        tag: my-wercker-tag
        registry: https://index.docker.io/v1/
        cmd: node /pipeline/source/server.js
        
deploy:
  steps:
    - script:
        name: webhook
        code: 
         curl -k -X POST https://140.86.13.180/api/v2/deployments/tadakamalla-mynode-20170831-195116/webhook/restart?token=jdxtnokb
