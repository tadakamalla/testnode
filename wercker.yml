box: tadakamalla/mynode
build:
  steps:
    - npm-install
push:
  steps:
    - internal/docker-push:
        username: $USERNAME
        password: $PASSWORD
        ports: "8085"
        tag: my-wercker-tag
        repository: tadakamalla/mynode
        registry: https://index.docker.io/v1/
        cmd: npm start
deploy:
  steps:
    - script:
        name: webhook
        code: 
         curl -k -X POST https://140.86.13.180/api/v2/deployments/tadakamalla-mynode-20170829-123914/webhook/restart?token=khojlcuo
