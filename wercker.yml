box: node
build:
   steps:
      - npm-install
image:
  step:
     - docker build -t tadakamalla/mynode .          

push:
  steps:
    - internal/docker-push:
        username: $USERNAME
        password: $PASSWORD
        tag: my-wercker-tag
        repository: tadakamalla/mynode
        registry: https://registry.hub.docker.com
        ports: "8085, 8085"
deploy:
  steps:
    - script:
        name: webhook
        code: 
         curl -k -X POST https://140.86.35.3/api/v2/deployments/tadakamalla-mynode-20170824-204108/webhook/restart?token=hssnkcpr
