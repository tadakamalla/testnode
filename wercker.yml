box: node
build:
  steps:
    - npm-install
push:
  steps:
    - internal/docker-push:
        username: $USERNAME
        password: $PASSWORD
        repository: tadakamalla/mynode
        tag: latest
        registry: https://hub.docker.com/u/tadakamalla/
        cmd: node /pipeline/source/server.js
        
deploy:
  steps:
    - script:
        name: webhook
        code: 
         curl -k -X POST https://140.86.41.49/api/v2/deployments/tadakamalla-mynode-20171017-125152/webhook/restart?token=wugoaboc
